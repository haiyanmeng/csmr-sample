# ROOT_REPO/namespaces/bookstore/reposync.yaml
apiVersion: configsync.gke.io/v1alpha1
kind: RepoSync
metadata:
  name: repo-sync
  namespace: bookstore
spec:
  sourceFormat: unstructured
  git:
    repo: https://github.com/haiyanmeng/bookstore-ns-repo
    branch: main
    auth: none
    #secretRef:
    #name: git-creds
